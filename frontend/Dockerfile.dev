FROM node:22-alpine

WORKDIR /app

# Install dependencies first
COPY package*.json ./
RUN npm install

# Copy the rest of the code
COPY . .

# Expose the dev port
EXPOSE 5174

# Run with host flag to allow access from outside the container
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
